import 'package:flutter/material.dart';

class Transist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Transist"),
      color: Colors.yellow,
    );
  }
}
