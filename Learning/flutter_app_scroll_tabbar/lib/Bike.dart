import 'package:flutter/material.dart';

class BikeTravel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Bike"),
      color: Colors.orange,
    );
  }
}
